# Throwaway fixture apps live here; never committed (PLAN.md §1, §10).
/workspace/*
!/workspace/.gitkeep

# Per-run eval verdicts (regenerated by `node evals/run.mjs`).
/evals/results/

# Packaged skill artifact — rebuild with:
#   python -m scripts.package_skill <this-repo>/skill  (from the skill-creator skill dir)
# then rename skill.skill → screen-list.skill (packager names by folder).
*.skill

# Skill-dev tooling from this repo's own Claude Code sessions — never committed.
/.agents/
/skills-lock.json
/.playwright-mcp/

# macOS cruft
.DS_Store
